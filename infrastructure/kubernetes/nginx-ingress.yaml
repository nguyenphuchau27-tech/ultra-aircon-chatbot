apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aircon-ingress
spec:
  rules:
    - host: api.aircon.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 80
